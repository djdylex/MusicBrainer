#include <iostream>
#include <vector>
#include <Eigen/Dense>

using Eigen::MatrixXd;

// Music sequences are over two octaves, 12 note equal temperament. 24 notes in total. C to B

int main()
{
	int sequence[16] = { 64, 0, 0, 0, 57, 0, 0, 0, 67, 0, 0, 65, 0, 0, 61, 0}; // 0 represents no note, it's all in 1/16th notes
}

MatrixXd generateSegments() {
	
}